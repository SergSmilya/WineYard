import { info } from "../theme/palette";

// NumberPackBtnComp & StaticQuantityCollectionComp
export const btnStyle = {
    display: "inline-flex",
    padding: "6px 10px",
    justifyContent: "center",
    alignItems: "center",
    gap: "10px",
    border: `1px solid ${info.dark}`,
    borderRadius: "11px",
}

export const flexCenter = {
    display: 'flex',
    justifyContent: "center",
    alignItems: "center",
}